{
  "name": "resize-api",
  "version": "0.0.1",
  "description": "Api resize image",
  "main": "server.js",
  "scripts": {
    "dev": "export $(cat .env | xargs) && env && cross-env NODE_ENV=development nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fernando Lopez",
  "license": "ISC",
  "dependencies": {
    "connect-mongo": "^3.2.0",
    "cross-env": "^7.0.2",
    "express": "^4.17.1",
    "lodash": "^4.17.15",
    "mongoose": "^5.9.21",
    "multer": "^1.4.2",
    "nodemon": "^2.0.4",
    "sharp": "^0.24.1"
  }
}
